<div class='container'>

  <h1>Restaurants</h1>

  <% @restaurants.each do |restaurant| %>
    <p><%= restaurant.name%> <%= link_to "See Details", restaurant_path(restaurant.id), class: "btn btn-primary"%></p>
    <br>
  <% end %>
  <%= link_to 'Add a new restaurant', new_restaurant_path, class: "btn btn-primary"%>
</div>
